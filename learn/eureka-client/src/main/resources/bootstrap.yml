eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: client
  cloud:
    config:
      discovery:
        service-id: CONFIG-SERVER
        enabled: true
      profile: dev